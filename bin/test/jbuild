(executable
 ((name        main)
  (public_name mdx-test)
  (package     mdx)
  (modes       (byte))  ;; no native code toplevel libs yet
  (link_flags  (-linkall))
  (libraries   (cli compiler-libs.toplevel findlib.top threads mdx.top))))
