(library
 ((name        mdx_top)
  (public_name mdx.top)
  (modes       (byte))  ;; no native code toplevel libs yet
  (libraries   (unix compiler-libs.toplevel findlib.top
                threads mdx ppx_tools compiler-libs))))
